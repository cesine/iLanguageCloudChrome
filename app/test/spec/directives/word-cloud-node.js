// 'use strict';

// describe('Directive: wordCloudNode', function () {

//   // load the directive's module
//   beforeEach(module('wordCloudApp'));

//   var element,
//     scope;

//   beforeEach(inject(function ($rootScope) {
//     scope = $rootScope.$new();
//   }));

//   it('should make hidden element visible', inject(function ($compile) {
//     element = angular.element('<word-cloud-node></word-cloud-node>');
//     element = $compile(element)(scope);
//     expect(element.text()).toBe('this is the wordCloudNode directive');
//   }));
// });
